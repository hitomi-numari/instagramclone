<!DOCTYPE html>
<html>
  <head>
    <title>everydayphoto</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/11c5374270.js" crossorigin="anonymous"></script>
  </head>
  <body>

      <header>
        <nav class="navbar navbar-default navbar-fixed-top">
        	<div class="container-fluid">
        		<div class="navbar-header">
        			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbarEexample7">
        				<span class="sr-only">everydayphoto</span>
        				<span class="icon-bar"></span>
        				<span class="icon-bar"></span>
        				<span class="icon-bar"></span>
        			</button>
        			<a class="navbar-brand" href="#">everydayphoto</a>
        		</div>

        		<div class="collapse navbar-collapse" id="navbarEexample7">
        			<ul class="nav navbar-nav">
                <% if logged_in? %>
                  <li><%= link_to "投稿一覧", pictures_path %></li>
                  <li><%= link_to '投稿する', new_picture_path %></li>
                  <li><%= link_to "マイページ", user_path(current_user.id) %></li>
                  <li><%= link_to "ログアウト", session_path(current_user.id), method: :delete %></li>
                  <li>
                    <p class="navbar-text navbar-right">ようこそ <%= current_user.name %> さん。</p>
                  </li>
                <% else %>
                  <li>
                    <%= link_to "ログインorサインアップ", new_session_path %>
                  </li>
                <% end %>
          		</ul>
        		</div>
        	</div>
        </nav>
      </header>

    <div class="container">
      <% flash.each do |key, value| %>
        <div class="alert <%= bootstrap_class_for(key) %>" ><%= value %></div>
      <% end %>
      <%= yield %>
    </div>

    <footer class="footer">
      <p class="text-muted text-center">運営会社everydayphoto株式会社</p>
      <p class="text-muted text-center">© 2015-2016 Copyright MyTweet Corp.</p>
    </footer>

  </body>
</html>
